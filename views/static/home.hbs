<div>
  {{#if posts}}
    <ul>
      {{#each posts as |post| }}
        <li>
          <h2>
            <a href="/posts/{{post.id}}">{{post.title}}</a>
          </h2>
        </li>
      {{/each}}
    </ul>
    {{else}}
      <p>We haven't published any posts yet.</p>
    {{/if}}
</div>